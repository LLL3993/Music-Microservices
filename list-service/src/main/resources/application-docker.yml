spring:
  datasource:
    url: jdbc:mysql://list-mysql:3306/list_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

services:
  user:
    base-url: http://user-service:8081
  meta:
    base-url: http://meta-service:8082

